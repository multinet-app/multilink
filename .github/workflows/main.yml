name: CI

on: 
  pull_request:
    branches:
    - master

job:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@v1
      with:
        node-version: '10.x'
    - name: test-install
    - run: yarn install
